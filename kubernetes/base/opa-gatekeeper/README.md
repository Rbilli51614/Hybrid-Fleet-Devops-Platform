# opa-gatekeeper

Gatekeeper install + constraint templates applied identically to both clusters.

**Status:** not yet built — see Phase 7 in the [top-level roadmap](../../../README.md#build-roadmap).
