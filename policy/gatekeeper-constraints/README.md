# gatekeeper-constraints

Constraint instances enforced org-wide (e.g. required labels, disallowed privileged pods).

**Status:** not yet built — see Phase 7 in the [top-level roadmap](../../README.md#build-roadmap).
